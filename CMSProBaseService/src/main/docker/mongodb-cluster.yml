version: '2'
services:
  cmsprobaseservice-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/cmsprobaseservice-mongodb-config --bind_ip 0.0.0.0
  cmsprobaseservice-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  cmsprobaseservice-mongodb-config:
    image: mongo:4.2.7
    container_name: cmsprobaseservice-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
